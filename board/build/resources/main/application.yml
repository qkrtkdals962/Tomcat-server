spring:
  datasource:
    url: jdbc:mysql://localhost:3307/board
    username: root
    password: wndr127#
    driver-class-name: com.mysql.cj.jdbc.Driver

jpa:
  hibernate:
    ddl-auto: create
  show-sql: true
  properties:
    hibernate:
      format_sql: true